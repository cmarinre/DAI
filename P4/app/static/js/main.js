


/*
.then(filas => {  
    // arrow function
    filas.forEach(fila => {     // bucle ES6, arrow function
        //NO ESTÁ ENTRANDO AQUÍ
        alert('wow')
        i++
        recetas.push(fila)      // se guardan para después sacar cada una             
        // ES6 templates
     //   html_str += '<div>ALGO</div>'
    });
   })
*/